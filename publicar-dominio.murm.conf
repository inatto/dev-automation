#!/usr/bin/env bash
# Edite somente este arquivo para publicar outro domínio.

REMOTE_USER="ubuntu"
REMOTE_HOST="44.219.174.82"
SSH_KEY="/home/daniel/amazon.ssh"

# Nome exclusivo do arquivo em sites-available/sites-enabled.
SITE_NAME="murmurinho.com"

# Identificação do aplicativo usada para impedir falso positivo de site errado.
APP_NAME="site-murm"

# O primeiro domínio é o principal e dá nome ao certificado.
DOMAINS=(
  "murmurinho.com"
  "www.murmurinho.com"
)

# A aplicação já deve estar respondendo no servidor antes da publicação.
UPSTREAM_HOST="127.0.0.1"
UPSTREAM_PORT="3002"

SSL_EMAIL="danielmaiax@gmail.com"
ISSUE_SSL="true"

# Espelho do servidor dentro do repositório sind-infra.
# O caminho é relativo à pasta deploy.
LOCAL_SERVER_DIR="../sind-amazon/lightsail/${REMOTE_HOST}/server"

CLIENT_MAX_BODY_SIZE="20m"
PROXY_READ_TIMEOUT="60s"
