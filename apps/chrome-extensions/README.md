# Extensões do Chrome

Cada extensão deve ficar em uma pasta própria dentro deste diretório, contendo o seu `manifest.json` na raiz.

Extensão disponível:

- `chatgpt-too-many-requests/` — remove automaticamente a janela visual “Too many requests” do ChatGPT ao clicar em “Got it”.
